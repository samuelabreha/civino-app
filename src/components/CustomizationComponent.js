import React from 'react';

const CustomizationComponent = () => {
    // Logique pour permettre la personnalisation de l'interface utilisateur
    return (
        <div>
            <h2>Personnalisation de l'interface</h2>
            {/* Ajoutez ici les options de personnalisation */}
        </div>
    );
};

export default CustomizationComponent;
